import { Navigate } from "react-router-dom";

// Esta página foi consolidada em /configuracoes para evitar duplicação.
const Clinicas = () => <Navigate to="/configuracoes" replace />;

export default Clinicas;
